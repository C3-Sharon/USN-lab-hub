# 2026-W39 后端执行台账：项目档案、成员与权限

## 使用规则

- 状态只使用 `PLANNED`、`IN_PROGRESS`、`VERIFIED`、`BLOCKED`、`SKIPPED`。
- 同一时间只能有一个 `IN_PROGRESS`。
- 本台账记录阶段结果和证据，不堆砌逐条命令。
- Flyway、权限、事务和幂等方案在实际改代码前必须经过用户确认。

## 1. 本周边界

目标：完成创建项目、分页查询、项目详情、添加成员和工作台项目摘要，使“创建项目 -> 自动成为 OWNER -> 邀请成员 -> 成员看到项目”可以真实联调。

不做：里程碑、任务、图片上传、项目编辑/删除、状态变更接口、退出/移除成员、角色变更、OWNER 转让、Redis、微服务和复杂审批。

生效契约：`docs/contracts/PROJECT_WORKSPACE_API.md`。

## 2. 当前检查点

- 当前块：B3 项目创建与查询
- 状态：PLANNED（等待用户确认进入）
- 基线：`origin/dev` commit `1f820a3`
- 分支目标：`feature/m01-w03-backend-project-members`
- 下一阶段门：向用户展示 V7 数据保留、权限和幂等方案并取得确认

## 3. 工作块总表

| 工作块 | 结果 | 验证 | 状态 |
|---|---|---|---|
| B1 契约与执行基线 | 消除 REVIEW/待确认矛盾，记录边界和兼容规则 | 文档差异检查；无业务代码变化 | VERIFIED |
| B2 数据迁移与历史兼容 | V7 扩展 lab_project、创建 lab_project_member、保留旧 IoT 数据 | Flyway 测试 + 真实 MySQL 迁移检查 | VERIFIED |
| B3 项目创建与查询 | 创建者事务内成为 OWNER；列表/详情按权限返回 | 服务测试、Controller 契约测试、唯一冲突测试 | PLANNED |
| B4 成员幂等与工作台 | 添加成员权限/幂等/归档拒绝；projects 区域 READY | 权限与失败路径测试、工作台相邻回归 | PLANNED |
| B5 收口与联调交接 | 全量测试、打包、真实 MySQL 主流程和旧 IoT 回归 | 单次全量测试、单次打包、联调记录 | PLANNED |

### B1：契约与执行基线

- 预期结果：接口状态均为 FROZEN，14 项确认结论可直接映射为测试。
- 修改范围：W39 周文档、项目工作台契约、本执行台账。
- 契约影响：澄清既有契约，不新增接口。
- 完成条件：不存在会改变后端行为的“待确认”项，且旧 `power-monitor` 兼容例外明确。

### B2：数据迁移与历史兼容

- 预期结果：新增 V7 迁移；现有项目和 `/api/iot/public/projects/power-monitor` 可继续工作。
- 进入条件：用户确认迁移、权限和幂等方案。
- 风险：HIGH，涉及 Flyway、历史数据和唯一约束。

### B3：项目创建与查询

- 预期结果：四类响应字段与分页、过滤、稳定排序符合冻结契约。
- 进入条件：B2 VERIFIED。
- 风险：HIGH，涉及项目级权限和创建事务。

### B4：成员幂等与工作台

- 预期结果：同角色重试返回 200，不同角色返回 409；归档拒绝；工作台返回真实项目。
- 进入条件：B3 VERIFIED。
- 风险：HIGH，涉及权限覆盖、幂等竞争和聚合回归。

### B5：收口与联调交接

- 预期结果：干净环境可复现，前端可以按真实 API 联调。
- 进入条件：B4 VERIFIED。
- 风险：不新增设计，只验证已确认行为。

## 4. 决策与偏差记录

- 2026-09-26：确认 14 项契约决策；纠正文档外层 FROZEN、接口内部 REVIEW 的矛盾。
- 2026-09-26：确认 `power-monitor` 是历史项目编号，`PM-001` 是设备编号；新项目编号规则不追溯破坏历史数据。
- 2026-09-26：当前工具无法写共享 `.git` 分支引用；文件在隔离工作区修改，分支建立/提交状态需在 B1 完成时据实记录。
- 2026-09-26：用户已建立 `feature/m01-w03-backend-project-members`，B1 分支阻塞解除。
- 2026-09-26：用户确认 V7 采用增量迁移、历史编码兼容、逻辑引用约束和数据库唯一键幂等方案；待 B1 独立提交后进入 B2。
- 2026-09-26：B1 内容检查通过，但当前 Agent 无权创建共享 Git index lock，提交需由用户在本机 PowerShell 执行。
- 2026-09-26：用户完成 B1 提交 `056b385` 并推送远端，B1 VERIFIED，B2 开始。
- 2026-09-26：V7 采用加列和新表的增量迁移，不收缩 project_code、不增加 CHECK、不修改历史 `power-monitor` 编号。
- 2026-09-26：`lab_project_member` 使用 `(project_id, user_id)` 唯一键保证成员幂等基础，按用户/项目和项目/角色建立查询索引，不增加会改变既有用户删除语义的物理外键。
- 2026-09-26：Codex 长工作区路径导致 Windows Maven 测试类路径异常；验证时临时映射短盘符 W:，未修改 pom 或业务配置。
- 2026-09-26：真实 MySQL 只使用独立临时库 `usn_hub_w39_migration_test`，验证后已执行删除；未迁移或改动现有 `usn_hub`。

## 5. 测试与证据

| 工作块 | 命令/检查 | 实际结果 |
|---|---|---|
| B1 | `git diff --check`；契约关键词检查；业务代码边界检查 | 通过；无后端契约 REVIEW/待确认残留；无业务代码或迁移改动。Git 仅提示行尾转换，不影响内容 |
| B2 | `ProjectWorkspaceMigrationTest`（H2 + MySQL 8）；`GlobalRoleMigrationTest,IotWeek5PublicIntegrationTest` | MySQL/H2 2/2 通过；相邻迁移和旧 IoT 3/3 通过；旧项目、OWNER 回填、唯一索引均验证。V1/V5 的 VALUES() 弃用警告为既有迁移债务，V7 未新增该用法 |
| B3 | 项目服务与 Controller 定向测试 | 待执行 |
| B4 | 成员、权限、工作台定向测试 | 待执行 |
| B5 | `mvn test`；`mvn -DskipTests package`；真实场景 | 待执行 |

## 6. PR 就绪检查

- [ ] 全部工作块 VERIFIED
- [ ] 生效契约与实现一致
- [ ] 真实 MySQL 迁移、唯一约束和事务证据齐全
- [ ] 旧鉴权、工作台和 IoT 相邻回归通过
- [ ] 完整测试只在 PR 就绪前执行一次
- [ ] 启动、迁移、curl 和前端联调说明齐全

## 7. 最终交接

待 B5 完成后填写实际提交、PR、接口、迁移、测试结果、已知风险和前端联调步骤。
